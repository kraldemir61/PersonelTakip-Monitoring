<Window x:Class="PersonelTakip.Views.CihazGecmisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Cihaz Hareket Geçmişi" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="Cihaz Hareket Geçmişi" FontSize="22" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="{Binding CihazBilgi}" FontSize="13" Foreground="#64748B" Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Column="1" CornerRadius="18" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Height="36" Width="220" VerticalAlignment="Center">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="32"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.4" FontSize="11"/>
                    <TextBox x:Name="SearchBox" Grid.Column="1" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" 
                             Padding="2,0,10,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" FontSize="12"/>
                    <TextBlock Grid.Column="1" Text="Arama..." Foreground="#94A3B8" VerticalAlignment="Center" 
                               IsHitTestVisible="False" Margin="5,0,0,0" FontSize="12">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
            <DataGrid ItemsSource="{Binding HareketlerView}" AutoGenerateColumns="False" IsReadOnly="True" 
                      HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0"
                      Background="White" RowHeight="40" AlternationCount="2">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Setter Property="Foreground" Value="#FFA2172A"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F1F5F9"/>
                        <Style.Triggers>
                            <Trigger Property="AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E5E7EB"/>
                                <Setter Property="Foreground" Value="#1F2937"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#1F2937"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="TextBlock" x:Key="CenterLeftStyle">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="Margin" Value="10,0"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tarih" Binding="{Binding Tarih, StringFormat='dd.MM.yyyy HH:mm'}" Width="130" ElementStyle="{StaticResource CenterLeftStyle}"/>
                    <DataGridTextColumn Header="İşlem" Binding="{Binding IslemTuru}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource CenterLeftStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IslemTuru}" Value="Transfer">
                                        <Setter Property="Foreground" Value="#3B82F6"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IslemTuru}" Value="Arıza">
                                        <Setter Property="Foreground" Value="#EF4444"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Nereden" Binding="{Binding NeredenSantiyeKod}" Width="90" ElementStyle="{StaticResource CenterLeftStyle}"/>
                    <DataGridTextColumn Header="Nereye" Binding="{Binding NereyeSantiyeKod}" Width="90" ElementStyle="{StaticResource CenterLeftStyle}"/>
                    <DataGridTextColumn Header="Yapan" Binding="{Binding KullaniciAdi}" Width="90" ElementStyle="{StaticResource CenterLeftStyle}"/>
                    <DataGridTextColumn Header="Açıklama" Binding="{Binding Aciklama}" Width="*" ElementStyle="{StaticResource CenterLeftStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Button Grid.Row="2" Content="Kapat" Command="{Binding KapatCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                Width="100" Height="36" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
